#include <stdio.h>
     int main() {
	int count = 0, a = 0, b = 1;
	for (;;) {
		printf("#");
	if (count = a % 2 ? a++ : a-- ? a = 0 : ++b ? b = 2 : b++){
            printf("%d", b);
			break;
    }
		printf("%d", ++count >> 1);
	}
	return 0;
}